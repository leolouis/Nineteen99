name: nineteen99
description: A Silicon Valley grade regulatory-aware sequential UPI splitting application.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0          # High-performance asynchronous HTTP networking
  google_fonts: ^6.1.0  # Premium typography layout
  uuid: ^4.3.3          # Cryptographic unique transaction IDs
  mobile_scanner: ^5.0.0 # High-speed camera scanner utility for QR codes

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
